"""Configuration for Seeweb provisioning."""

from typing import Any, Dict

def bootstrap_instances(config: Dict[str, Any]) -> Dict[str, Any]:
    """Bootstrap instances for Seeweb - no special config needed."""
    return config 